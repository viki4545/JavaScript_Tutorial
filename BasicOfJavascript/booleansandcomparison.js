// booleans & comparison operator

// booleans 
    // true or false
    
// comparison operator

    // ==(equal to)  	
    // ===(equal value and equal type) 		
    // != (not equal) 		
    // !==(not equal value or not equal type) 
    // >(greater than) 		
    // <(less than) 	 	
    // >=(greater than or equal to) 		
    // <=(less than or equal to)

    // let num1 = 5;
    // let num2 = 7;
    // console.log(num1 > num2);

// == vs ===
    let num1 = "7";
    let num2 = 7;

    // it happens only in javascript not in other programming langauage
    // that == does not check the datatype it only checks the value are equal or not
    // and in === it check both the value and datatype that are equal or not
    // console.log(num1 == num2);

// != vs !==

    // it happens only in javascript not in other programming langauage
    // that != does not check the datatype it only checks the value are not equal to
    // and in !== it check both the value and datatype that are not equal to.
     // console.log(num1 != num2);
    console.log(num1 !== num2);